{
	"private": true,
	"name": "svgproto",
	"version": "0.0.0",
	"license": "MIT",
	"browserslist": [
		"> 1%",
		"last 2 versions"
	],
	"scripts": {
		"start": "per-env",
		"start:production": "npm run -s serve",
		"start:development": "npm run -s dev",
		"build": "preact build --no-prerender --no-sw",
		"serve": "preact build && preact serve",
		"dev": "preact watch"
	},
	"devDependencies": {
		"postcss-loader": "^3.0.0",
		"postcss-preset-env": "^5.1.0",
		"preact-cli": "^3.0.0-rc.9",
		"simple-svg-loader": "file:./lib/simple-svg-loader",
		"simple-svg-collision": "file:./lib/simple-svg-collision",
		"style-loader": "^0.23.1",
		"worker-plugin": "^3.2.0"
	},
	"dependencies": {
		"classnames": "^2.2.6",
		"flubber": "^0.4.2",
		"preact": "^10.3.2",
		"preact-router": "^3.2.1",
		"rematrix": "^0.3.0"
	}
}
